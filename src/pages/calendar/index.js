import React from 'react';
import _ from 'lodash';
import createReactClass from 'create-react-class';

export const CalendarPage = createReactClass({
    render() {
        return (
            <div>
                Calendar
            </div>
        );
    },
});
